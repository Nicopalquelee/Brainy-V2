// jwt.strategy.ts
// This file was previously using passport-based strategy which required
// external packages. To avoid adding those dependencies here we keep a
// placeholder so the project compiles. The auth flow uses a custom
// JwtAuthGuard in jwt-auth.guard.ts instead.

export const DeprecatedJwtStrategyPlaceholder = true;
